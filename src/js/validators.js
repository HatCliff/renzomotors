const evaluarstrenght = (input)=>{
    let fitness = 0;
    if (input.match(/a-z/)) {
        console.log("MATCH");
    }
    /*
    if (condition) {
        
    }
    if (condition) {
        
    }
    if (condition) {
        
    }
    */
    switch (fitness) {
        case value:
            
            break;
    
        default:
            break;
    }
}


export default evaluarstrenght;